name: Release
on:
  push:
    tags:
      - "v[0-9]*.[0-9]*.[0-9]*"
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: loilo-inc/actions-semantic-release@v1
